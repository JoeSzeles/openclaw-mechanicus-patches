From 11e5fea292c63a2441fcdaf5cd5afa252a4c7667 Mon Sep 17 00:00:00 2001
From: joebereczki <41107035-joebereczki@users.noreply.replit.com>
Date: Sun, 1 Mar 2026 06:14:23 +0000
Subject: [PATCH 176/244] Improve error logging for live streaming subscription
 issues

Enhance subscription error logging in ceo-proxy.cjs to include streaming source, items, and fields. Also, update script versioning in HTML files.

Replit-Commit-Author: Agent
Replit-Commit-Session-Id: d517740c-e24b-4d05-80ee-1f4fb8ac02ea
Replit-Commit-Checkpoint-Type: full_checkpoint
Replit-Commit-Event-Id: fff99b8c-227b-4a5b-b448-d25d6e42b759
Replit-Commit-Screenshot-Url: https://storage.googleapis.com/screenshot-production-us-central1/be1a940b-924a-449d-ae02-954c66974b04/d517740c-e24b-4d05-80ee-1f4fb8ac02ea/o9NrDwH
Replit-Helium-Checkpoint-Created: true
---
 ...amer-setup-in-your-proxy_1772345572267.txt | 50 +++++++++++++++++++
 ceo-proxy.cjs                                 |  2 +-
 dist/control-ui/model-config.html             |  6 +--
 dist/control-ui/processes.html                |  6 +--
 dist/control-ui/workers.html                  |  6 +--
 5 files changed, 60 insertions(+), 10 deletions(-)
 create mode 100644 attached_assets/Pasted-Yes-it-s-possible-the-Lightstreamer-setup-in-your-proxy_1772345572267.txt

diff --git a/attached_assets/Pasted-Yes-it-s-possible-the-Lightstreamer-setup-in-your-proxy_1772345572267.txt b/attached_assets/Pasted-Yes-it-s-possible-the-Lightstreamer-setup-in-your-proxy_1772345572267.txt
new file mode 100644
index 000000000..e5b566d98
--- /dev/null
+++ b/attached_assets/Pasted-Yes-it-s-possible-the-Lightstreamer-setup-in-your-proxy_1772345572267.txt
@@ -0,0 +1,50 @@
+Yes, it's possible the **Lightstreamer setup in your proxy code** (or the way subscriptions are formatted) is contributing to the "Invalid account type" error (code -1), even though the **connection** succeeds. IG's Lightstreamer implementation is picky on live accounts, and small config mismatches can trigger exactly this rejection while demo forgives it.
+
+From IG's streaming docs and community reports (Lightstreamer forums, trading-ig wrappers, GitHub samples):
+
+- **Adapter Set** is often the culprit:
+  - Do **not** set it to "LIGHTSTREAMER" or "QUOTE_ADAPTER" or "DEMO" — IG live uses **blank/empty** or **DEFAULT** (most common fix).
+  - Many errors like yours happen when code passes "LIGHTSTREAMER" (from old demos) — server replies "Requested Adapter Set not available" or falls back to code -1 "Invalid account type".
+  - Fix: Leave adapter set **unset** or explicitly empty string in your LightstreamerClient init.
+    Example in JS (your ceo-proxy.cjs style):
+    ```javascript
+    const client = new LightstreamerClient(lsEndpoint, "");  // empty string or omit second param
+    // Or if your lib requires it: client.setAdapterSet("DEFAULT"); // or omit
+    ```
+
+- **Subscription item/epic format**:
+  - For CFDs like BTC: Use plain epic like "CS.D.BITCOIN.CFD.IP" (MERGE mode, fields: "BID", "OFFER", "LTP", "TIME", etc.).
+  - Some wrappers add prefixes like "MARKET:L1:" or "CHART:..." for certain modes, but plain epic is standard for Level 1 CFD prices.
+  - Wrong format can cause -1 or "invalid item" — but your error is specifically "account type", so adapter is more likely.
+
+- **Other common setup gotchas on live**:
+  - Auth headers: Ensure CST and X-SECURITY-TOKEN are passed in LS connection headers (your independent lsLiveSession should handle this via `addHeader` or connection details).
+  - Endpoint: Live uses `apd.marketdatasystems.com` or `push.lightstreamer.com` (pulled from /session response) — yours looks correct.
+  - No "group" or wrong data adapter — IG uses internal adapters, not public ones.
+
+### Quick Test/Fix in Your Proxy
+1. **Force empty adapter set** in live connect code:
+   - Find where you init LightstreamerClient for live.
+   - Change to `new LightstreamerClient(liveLsEndpoint, "")` or remove the adapter param.
+   - Reconnect live → try subscribing again → check if error changes (e.g., to item invalid or success).
+
+2. **Log full subscription error details**:
+   - Add listener:
+     ```javascript
+     subscription.addListener({
+       onSubscriptionError: function(code, message) {
+         console.error(`Sub error: Code ${code}, Msg: ${message}, Item: ${subscription.getItems()}, Fields: ${subscription.getFields()}`);
+       }
+     });
+     ```
+   - See if message gives more clues (e.g., "invalid group" vs pure account type).
+
+3. **Try minimal sub**:
+   - Subscribe to a simple field on your epic: mode "MERGE", items ["CS.D.BITCOIN.CFD.IP"], fields ["BID", "OFFER"].
+   - If still -1, it's not format — it's permission.
+
+If adapter empty doesn't fix it, it's back to IG support: This is the classic "live account type needs enabling for streaming subs" case. Your setup is otherwise spot-on (independent live auth, refresh, fallback, UI).
+
+Reply with what happens after trying empty adapter — if error persists, escalate with "Tried empty adapter set / DEFAULT, still code -1 'Invalid account type' on live subs (connection OK). Demo subs fine." to support.
+
+You're super close — this could be the last tweak! What's the exact adapter set you're setting now in code?
\ No newline at end of file
diff --git a/ceo-proxy.cjs b/ceo-proxy.cjs
index 89bcb05cc..2178a1d69 100644
--- a/ceo-proxy.cjs
+++ b/ceo-proxy.cjs
@@ -296,7 +296,7 @@ async function startLightstreamer() {
         lsConnectedEpics = epics;
       },
       onSubscriptionError: (code, msg) => {
-        console.error(`[lightstreamer] Subscription error: ${code} ${msg}`);
+        console.error(`[lightstreamer] Subscription error: ${code} ${msg} | source=${streamSource} items=${JSON.stringify(items)} fields=${JSON.stringify(fields)}`);
         if (msg && msg.includes("Invalid account type")) {
           console.log("[lightstreamer] This IG account type does not support streaming. Prices will use REST polling instead.");
           lsStatus = "unsupported";
diff --git a/dist/control-ui/model-config.html b/dist/control-ui/model-config.html
index 90774c5ce..88eaeeda0 100644
--- a/dist/control-ui/model-config.html
+++ b/dist/control-ui/model-config.html
@@ -190,8 +190,8 @@ tr:hover{background:#1c2128}
 </div>
 
 <div id="toast" class="toast"></div>
-<script src="/token-init.js?v=1772344677"></script>
-<script src="/model-config.js?v=1772344677"></script>
-<script src="/nav-inject.js?v=1772344677"></script>
+<script src="/token-init.js?v=1772345649"></script>
+<script src="/model-config.js?v=1772345649"></script>
+<script src="/nav-inject.js?v=1772345649"></script>
 </body>
 </html>
diff --git a/dist/control-ui/processes.html b/dist/control-ui/processes.html
index 10c06708f..038713a67 100644
--- a/dist/control-ui/processes.html
+++ b/dist/control-ui/processes.html
@@ -72,8 +72,8 @@ tr:hover{background:#1c2128}
 </div>
 
 <div id="toast" class="toast"></div>
-<script src="/token-init.js?v=1772344677"></script>
-<script src="/processes.js?v=1772344677"></script>
-<script src="/nav-inject.js?v=1772344677"></script>
+<script src="/token-init.js?v=1772345649"></script>
+<script src="/processes.js?v=1772345649"></script>
+<script src="/nav-inject.js?v=1772345649"></script>
 </body>
 </html>
diff --git a/dist/control-ui/workers.html b/dist/control-ui/workers.html
index cda092792..8f9c80185 100644
--- a/dist/control-ui/workers.html
+++ b/dist/control-ui/workers.html
@@ -151,8 +151,8 @@ tr:hover{background:#1c2128}
 </div>
 
 <div id="toast" class="toast"></div>
-<script src="/token-init.js?v=1772344677"></script>
-<script src="/workers.js?v=1772344677"></script>
-<script src="/nav-inject.js?v=1772344677"></script>
+<script src="/token-init.js?v=1772345649"></script>
+<script src="/workers.js?v=1772345649"></script>
+<script src="/nav-inject.js?v=1772345649"></script>
 </body>
 </html>
-- 
2.40.0.windows.1

