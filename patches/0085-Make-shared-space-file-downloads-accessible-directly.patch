From d1e05b28c8c18aee6f52879a15fe85039e24fb8f Mon Sep 17 00:00:00 2001
From: joebereczki <41107035-joebereczki@users.noreply.replit.com>
Date: Thu, 26 Feb 2026 05:45:27 +0000
Subject: [PATCH 085/244] Make shared space file downloads accessible directly
 from a browser

Adjust the shared space download endpoint to allow unauthenticated access, ensuring direct browser link clicks function similarly to existing CEO Workspace downloads.

Replit-Commit-Author: Agent
Replit-Commit-Session-Id: 9b6db5b8-bb27-4a35-8d77-7730eb3a7059
Replit-Commit-Checkpoint-Type: full_checkpoint
Replit-Commit-Event-Id: b8ab2479-ed4d-453d-9582-4a814f240a42
Replit-Commit-Screenshot-Url: https://storage.googleapis.com/screenshot-production-us-central1/be1a940b-924a-449d-ae02-954c66974b04/9b6db5b8-bb27-4a35-8d77-7730eb3a7059/XkTRMZL
Replit-Helium-Checkpoint-Created: true
---
 ceo-proxy.cjs | 30 +++++++++++++++---------------
 1 file changed, 15 insertions(+), 15 deletions(-)

diff --git a/ceo-proxy.cjs b/ceo-proxy.cjs
index 10b13d9a3..baa6bfaaa 100644
--- a/ceo-proxy.cjs
+++ b/ceo-proxy.cjs
@@ -671,6 +671,21 @@ function isInsideDir(fullPath, baseDir) {
 }
 
 async function handleSharedspace(req, res, p) {
+  if (req.method === "GET" && p.startsWith("/api/sharedspace/download/")) {
+    const fp = path.normalize(decodeURIComponent(p.slice("/api/sharedspace/download/".length)));
+    const full = path.resolve(SHAREDSPACE_DIR, fp);
+    if (!isInsideDir(full, SHAREDSPACE_DIR)) return json(res, 403, { error: "Forbidden" });
+    try {
+      const d = fs.readFileSync(full);
+      res.writeHead(200, {
+        "Content-Type": "application/octet-stream",
+        "Content-Disposition": `attachment; filename="${path.basename(fp)}"`,
+        "Access-Control-Allow-Origin": "*",
+      });
+      return res.end(d);
+    } catch { return json(res, 404, { error: "File not found" }); }
+  }
+
   const isGw = authGateway(req);
   const apiKey = authWorker(req);
   if (!isGw && !apiKey) return json(res, 401, { error: "Unauthorized" });
@@ -736,21 +751,6 @@ async function handleSharedspace(req, res, p) {
     return json(res, 201, { ok: true, path: fp });
   }
 
-  if (req.method === "GET" && p.startsWith("/api/sharedspace/download/")) {
-    const fp = path.normalize(decodeURIComponent(p.slice("/api/sharedspace/download/".length)));
-    const full = path.resolve(SHAREDSPACE_DIR, fp);
-    if (!isInsideDir(full, SHAREDSPACE_DIR)) return json(res, 403, { error: "Forbidden" });
-    try {
-      const d = fs.readFileSync(full);
-      res.writeHead(200, {
-        "Content-Type": "application/octet-stream",
-        "Content-Disposition": `attachment; filename="${path.basename(fp)}"`,
-        "Access-Control-Allow-Origin": "*",
-      });
-      return res.end(d);
-    } catch { return json(res, 404, { error: "File not found" }); }
-  }
-
   if (req.method === "DELETE" && p.startsWith("/api/sharedspace/")) {
     const sub = p.slice("/api/sharedspace/".length);
     if (!sub || sub === "read" || sub === "write" || sub === "mkdir" || sub === "download") {
-- 
2.40.0.windows.1

