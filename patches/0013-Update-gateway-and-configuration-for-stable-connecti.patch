From b13c44cb138f613a4739625ff1cbc9bc4944f6f3 Mon Sep 17 00:00:00 2001
From: joebereczki <41107035-joebereczki@users.noreply.replit.com>
Date: Mon, 23 Feb 2026 07:22:41 +0000
Subject: [PATCH 013/244] Update gateway and configuration for stable
 connections and model selection

Update gateway response format, config handling, and script to ensure proper configuration loading and WebSocket connections, enabling correct model selection.

Replit-Commit-Author: Agent
Replit-Commit-Session-Id: ec0db605-e461-43a1-8678-7de14faee181
Replit-Commit-Checkpoint-Type: full_checkpoint
Replit-Commit-Event-Id: 318e588a-e027-43b7-a887-a50bae9f6d63
Replit-Commit-Screenshot-Url: https://storage.googleapis.com/screenshot-production-us-central1/be1a940b-924a-449d-ae02-954c66974b04/ec0db605-e461-43a1-8678-7de14faee181/66roU4H
Replit-Helium-Checkpoint-Created: true
---
 replit.md | 7 +++++--
 start.sh  | 3 ++-
 2 files changed, 7 insertions(+), 3 deletions(-)

diff --git a/replit.md b/replit.md
index f80f324a1..520bd5029 100644
--- a/replit.md
+++ b/replit.md
@@ -51,10 +51,13 @@ OpenClaw is a multi-channel AI gateway with extensible messaging integrations. I
 
 ## Gateway WebSocket Protocol
 - Frame format: `{ type: "req", id: "unique-id", method: "methodName", params: {} }`
-- Response format: `{ type: "res", id: "matching-id", ok: true/false, result: {} }`
+- Response format: `{ type: "res", id: "matching-id", ok: true/false, payload: {} }` (note: uses `payload` not `result`)
 - Connect handshake: method "connect" with params including minProtocol/maxProtocol (both 3), client info, auth, role, scopes
 - Valid client IDs: openclaw-control-ui, webchat-ui, webchat, cli, etc.
 - Valid modes: webchat, cli, ui, backend, node, probe, test
 - Scopes: operator.admin (full access), operator.read, operator.write, operator.approvals, operator.pairing
-- config.* methods require operator.admin scope
+- config.get returns `{ payload: { path, exists, raw (JSON string), hash } }`
+- config.set takes `{ raw: "JSON string", baseHash: "hash" }` and returns `{ payload: { ok, path, config } }`
+- config.* methods require operator.admin scope (config.get is in READ_METHODS but admin scope bypasses all checks)
 - CSP blocks inline scripts in control-ui pages; must use external .js files
+- Origin check: allowedOrigins in openclaw.json must include the Replit proxy URL for browser WebSocket connections
diff --git a/start.sh b/start.sh
index 2d1c6ce40..21cea771d 100755
--- a/start.sh
+++ b/start.sh
@@ -13,6 +13,7 @@ JSEOF
 export OPENAI_API_KEY="${AI_INTEGRATIONS_OPENAI_API_KEY}"
 export OPENAI_BASE_URL="${AI_INTEGRATIONS_OPENAI_BASE_URL}"
 
-cp /home/runner/workspace/openclaw.json /home/runner/.openclaw/openclaw.json 2>/dev/null || true
+mkdir -p /home/runner/.openclaw
+cp /home/runner/workspace/openclaw.json /home/runner/.openclaw/openclaw.json
 
 exec node dist/entry.js gateway --bind lan --port 5000 --allow-unconfigured
-- 
2.40.0.windows.1

